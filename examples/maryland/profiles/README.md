# Project and shared profiles

Profiles keep broad estimates reusable without copying unexplained numbers between
tracks. This directory is project-local. Add another root in `project.toml` when
several projects should share one team-maintained profile library.

A profile may extend a built-in profile and replace only values the team knows
better. Every inherited physical value keeps units, provenance, and uncertainty.
