[profile]
id = "team.vehicle.baja_defaults_v1"
scope = "vehicle"
version = 1
description = "Example team-level estimates shared by this project's vehicles."
extends = ["builtin.vehicle.baja_generic_v1"]

[config.drivetrain.efficiency]
nominal = 0.9506
unit = "1"

[config.drivetrain.efficiency.source]
kind = "engineering_estimate"
reference = "replace with team dyno, component, or prior-season estimate"

[config.drivetrain.efficiency.uncertainty]
distribution = "normal"
role = "structural"
mode = 0.9506
standard_deviation = 0.0077