# Obstacle profiles

Obstacle profiles remain intentionally deferred until Phase 5 freezes each
obstacle equation, parameter meaning, unit, spatial application, and validity
range. Values such as a future impact coefficient will receive documented broad
defaults only after the mechanism contract is stable.
